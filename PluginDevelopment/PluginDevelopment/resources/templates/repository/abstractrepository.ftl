package generated.repository${package};

import generated.model${package}.${name};
import io.micronaut.data.repository.PageableRepository;

public interface Abstract${name}Repository extends PageableRepository<${name}, ${key}>{

}
